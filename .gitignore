# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VSCode — uncomment if you don't want to share launch configs
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android build artifacts
/android/app/debug
/android/app/profile
/android/app/release

# Signing keys — never commit these
key.properties
*.jks
*.keystore

# Environment / secrets — never commit
env/
.env
.env.*

# Internal docs — kept off GitHub to maintain exclusivity
docs/

# Route data — proprietary, not for public repo
# The app ships these baked into the APK/IPA, but we don't
# want the raw JSON walking around on GitHub
assets/data/stops.json
assets/data/schedules.json
assets/data/routes_wgs84.json

# Scratch / one-off assets
stitch_assets/
# build_runner generated code — regenerate with:
#   flutter pub run build_runner build --delete-conflicting-outputs
**/*.freezed.dart
**/*.g.dart

# scratch test files (not real tests)
test_arcgis.dart
test_camera.dart
